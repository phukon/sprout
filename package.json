{
  "name": "sprout",
  "module": "index.ts",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "sprout": "./dist/cli.js"
  },
  "private": true,
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --outdir dist --target bun --minify",
    "test": "bun test",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/**/*.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "license": "MIT",
  "author": "Riki Phukon",
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^14.0.2",
    "toml": "^3.0.0"
  }
}
